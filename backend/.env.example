# Banco de dados local (preencha se não usar DATABASE_URL)
DB_USER=
DB_HOST=
DB_DATABASE=
DB_PASSWORD=
DB_PORT=5432

# Banco de dados em produção (Railway, Supabase etc.) — substitui as vars acima quando preenchido
DATABASE_URL=

# E-mail (Gmail com senha de app)
EMAIL_USER=
EMAIL_PASS=
EMAIL_DESTINO=

# Servidor
PORT=3001

# URL pública do frontend em produção (ex: https://tecnosenior.com)
FRONTEND_URL=
